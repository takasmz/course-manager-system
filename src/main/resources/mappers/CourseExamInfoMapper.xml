<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursemanager.mapper.CourseExamInfoMapper">
  <resultMap id="BaseResultMap" type="com.coursemanager.model.CourseExamInfo">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Nov 18 17:21:50 CST 2018.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="teacher_id" jdbcType="VARCHAR" property="teacherId" />
    <result column="course_id" jdbcType="VARCHAR" property="courseId" />
    <result column="status" jdbcType="INTEGER" property="status" />
    <result column="show_answer_time" jdbcType="TIMESTAMP" property="showAnswerTime" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="expire_time" jdbcType="TIMESTAMP" property="expireTime" />
  </resultMap>

    <resultMap id="BaseResultMapDto" type="com.coursemanager.dto.CourseExamInfoDto">
        <result column="date" jdbcType="VARCHAR" property="date" />
        <result column="course_name" jdbcType="VARCHAR" property="courseName" />
        <result column="course_exam_name" jdbcType="VARCHAR" property="courseExamName" />
    </resultMap>

  <select id="selectByExamId" resultMap="BaseResultMap" parameterType="String">
    SELECT
        cei.course_id,
        cei.exam_number,
        cei.teacher_id,
        cei.show_answer_time,
        cei.create_time,
        cei.expire_time
    FROM
        course_exam_info cei
        LEFT JOIN exam_info ei ON cei.id = ei.course_exam_id
    WHERE
        ei.exam_id = #{examId}
	AND cei.`status` = 0
  </select>

    <select id="queryHistoryHomeworkList" parameterType="map" resultMap="BaseResultMapDto">
        SELECT
          CONCAT(ci.`year`,'学年第',ci.term,'学期') date,
          ci.course_name,
          CONCAT('第',cei.exam_number,'次作业') course_exam_name,
          cei.id
        FROM course_info ci
        LEFT JOIN course_exam_info cei ON ci.id = cei.course_id
        <where>
            <if test="year != null and year != ''">
                AND ci.year = #{year}
            </if>
            <if test="term != null and term != ''">
                AND ci.term = #{term}
            </if>
            <if test="courseName != null and courseName != ''">
                AND ci.course_name = #{courseName}
            </if>
        </where>
        ORDER BY date,ci.course_name
    </select>
</mapper>